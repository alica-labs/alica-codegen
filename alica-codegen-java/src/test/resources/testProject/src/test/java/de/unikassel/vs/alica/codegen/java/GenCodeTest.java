package de.unikassel.vs.alica.codegen;

import org.junit.Test;
import org.junit.Assert;

public class GenCodeTest {
    @Test
    public void testConstraintCreator() {
        Assert.assertTrue(true);
        Assert.assertFalse(false);
    }
}
